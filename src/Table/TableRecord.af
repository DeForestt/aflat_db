.needs <std>
import Row from "../Row";

unique class TableRecord {
    long pageId = pageId;
    int slotId = slotId;
    Row row = $row;

    fn init(long pageId, int slotId, Row &&row) -> Self {
        return my;
    };
};
