.needs <std>
import Record from "../Record";

unique class HeapRecord {
    long pageId = pageId;
    int slotId = slotId;
    Record record = $record;

    fn init(long pageId, int slotId, Record &&record) -> Self {
        return my;
    };
};
